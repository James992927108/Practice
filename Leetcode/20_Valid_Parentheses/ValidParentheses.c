#include <stdio.h>
#include <string.h>
#include <stdlib.h>

bool isValid(char * s){

}

int main()
{
    char* strs[] ={"flower","flow","flight"};
    s = "()"
    // char** temp;
    // temp = &strs[0];
    // printf("%s\n", *temp);

    int strsSize = 3;
    char* result;

    result = longestCommonPrefix(strs, strsSize);

    printf("%s\n", result);
    return 0;
}
